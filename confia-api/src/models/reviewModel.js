class Review {
  constructor(id, serviceId, clientId, rating, comment) {
    this.id = id;
    this.serviceId = serviceId;
    this.clientId = clientId;
    this.rating = rating;
    this.comment = comment;
  }
}

export default Review;
